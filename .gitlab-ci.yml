image: ubuntu:latest

variables:
  TF_ROOT: ${CI_PROJECT_DIR}
  TF_STATE_NAME: ${CI_PROJECT_NAME}-${CI_COMMIT_REF_SLUG}
  TERRAFORM_VERSION: "1.9.3"

cache:
  key: ${CI_COMMIT_REF_SLUG}
  paths:
    - ${TF_ROOT}/.terraform

before_script:
  - apt-get update && apt-get install -y wget unzip
  - wget https://releases.hashicorp.com/terraform/${TERRAFORM_VERSION}/terraform_${TERRAFORM_VERSION}_linux_amd64.zip
  - unzip terraform_${TERRAFORM_VERSION}_linux_amd64.zip
  - mv terraform /usr/local/bin/
  - terraform version
  - cd ${TF_ROOT}
  - rm -rf .terraform/providers
  - terraform init -backend-config="key=terraform-state/${TF_STATE_NAME}" -upgrade

stages:
  - validate
  - import
  - plan
  - apply

validate:
    stage: validate
    script:
        - terraform validate

import:
    stage: import
    script:
      - |
        if ! terraform state list | grep -q "module.s3_bucket.aws_s3_bucket.bucket_state_terraform"; then
          echo "Importing S3 bucket..."
          terraform import module.s3_bucket.aws_s3_bucket.bucket_state_terraform terraform-state-bucket-karthik || echo "S3 bucket import failed, it might already exist in Terraform state"
        else
          echo "S3 bucket exists"
        fi
      - |
        if ! terraform state list | grep -q "module.ec2.aws_instance.jenkins_ec2_instance"; then
          echo "Importing EC2 instance..."
          terraform import module.ec2.aws_instance.jenkins_ec2_instance i-0516179026a961679 || echo "EC2 instance import failed, it might already exist in Terraform state"
        else
          echo "EC2 already exists"
        fi
        fi
      - |
        if ! terraform show | grep -q "module.ec2.aws_key_pair.ec2_instance_public_key"; then
          terraform import module.ec2.aws_key_pair.ec2_instance_public_key aws_ec2_terraform
        else
          echo "Key pair already exists"
        fi
    allow_failure: true

plan:
    stage: plan
    script:
        - terraform plan -out=tfplan
    artifacts:
        paths:
          - tfplan

apply:
    stage: apply
    script:
        - terraform apply -auto-approve tfplan 
    when: manual
    only: 
        - main